machine:
  node:
    version: 7.9.0
compile:
  override:
    - npm run build
test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/reports
    - nyc --reporter=lcov --report-dir=$CIRCLE_TEST_REPORTS/reports yarn test
